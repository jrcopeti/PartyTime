<h1>Chatrooms</h1>
<% @chatrooms.each do |chatroom| %>
  <div class="chatroom">
    <h2><%= chatroom.name %></h2>
    <%= link_to chatroom.name, chatroom_path(chatroom) %>

    <!--    <p>Participants: <%#= chatroom.users.map(&:name).join(", ") %></p>-->
<!--    <p>Last message: <%#= chatroom.messages.last.content %></p>-->
    <!-- ... -->
  </div>
<% end %>