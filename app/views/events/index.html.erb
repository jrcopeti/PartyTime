<% content_for :meta_title, "Maps on #{DEFAULT_META["meta_product_name"]}" %>
<% content_for :meta_description, "Find the nightclubs in town and dance the night away to the best beats. Get exclusive VIP access, bottle service, and more. Come join the party at PartyTime!" %>
<% content_for :meta_image, asset_url('/assets/logo.jpg') %>

<div class="event-title">
  <h1>Map</h1>
</div>

<div class="map">
  <div
    style="width: 100%; height: 520px;"
    data-controller="map" data-map-markers-value="<%= @markers.to_json %>"
    data-map-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>">
  </div>
</div>
